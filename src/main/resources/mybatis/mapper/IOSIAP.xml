<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.$51zhiyuan.development.kidbridge.dao.IOSIAP">

    <select id="list" parameterType="IOSIAP" resultType="IOSIAP">
        SELECT
            `iosiap`.`name`,
            `iosiap`.`product`
        FROM
            `t_ios_iap` AS `iosiap`
        WHERE
            `iosiap`.`del_flag` = FALSE
    </select>

</mapper>